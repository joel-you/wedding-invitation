# 모바일 청첩장 배포 가이드

이 가이드는 모바일 청첩장을 GitHub Pages에 무료로 배포하는 방법을 설명합니다.

## 🚀 GitHub Pages로 배포하기 (추천)

### 1단계: GitHub 저장소 생성

1. [GitHub](https://github.com)에 로그인
2. 우측 상단의 "+" 버튼 클릭 → "New repository" 선택
3. 저장소 정보 입력:
   - Repository name: `wedding-invitation` (원하는 이름)
   - Public 선택 (무료 호스팅을 위해 필수)
   - "Create repository" 클릭

### 2단계: 코드를 GitHub에 푸시

터미널에서 프로젝트 폴더로 이동 후 실행:

```bash
# Git 초기화 (이미 되어있으면 생략)
git init

# 모든 파일 추가
git add .

# 커밋 생성
git commit -m "Initial commit: Wedding invitation website"

# GitHub 저장소 연결 (YOUR_USERNAME을 실제 GitHub 사용자명으로 변경)
git remote add origin https://github.com/YOUR_USERNAME/wedding-invitation.git

# 메인 브랜치로 푸시
git branch -M main
git push -u origin main
```

### 3단계: GitHub Pages 활성화

1. GitHub 저장소 페이지로 이동
2. **Settings** 탭 클릭
3. 왼쪽 메뉴에서 **Pages** 클릭
4. Source 섹션에서:
   - Branch: `main` 선택
   - Folder: `/ (root)` 선택
   - **Save** 클릭
5. 몇 분 후 페이지 상단에 배포 URL이 표시됩니다:
   ```
   https://YOUR_USERNAME.github.io/wedding-invitation/
   ```

### 4단계: 네이버 지도 API 설정 업데이트

배포 후 네이버 지도가 작동하려면 도메인을 등록해야 합니다:

1. [네이버 클라우드 플랫폼 콘솔](https://console.ncloud.com/) 접속
2. AI·NAVER API → Maps → 등록한 Application 선택
3. **Web 서비스 URL** 섹션에 추가:
   ```
   https://YOUR_USERNAME.github.io
   ```
4. 저장

### 5단계: 배포 완료!

- 웹사이트 URL: `https://YOUR_USERNAME.github.io/wedding-invitation/`
- 변경사항이 있을 때마다 git push하면 자동으로 재배포됩니다

```bash
# 변경사항 푸시 (자동 배포됨)
git add .
git commit -m "Update wedding details"
git push
```

---

## 🌐 다른 배포 옵션

### Vercel (추천 #2)

**장점**: 매우 빠른 배포, 자동 HTTPS, 커스텀 도메인 무료

```bash
# Vercel CLI 설치
npm install -g vercel

# 프로젝트 폴더에서 실행
vercel

# 질문에 답하면 자동 배포
```

- 배포 URL: `https://프로젝트명.vercel.app`
- 대시보드: https://vercel.com/dashboard

### Netlify

**장점**: 드래그 앤 드롭 배포, 자동 HTTPS, 무료 폼 처리

1. [Netlify](https://www.netlify.com/)에 로그인
2. "New site from Git" 클릭
3. GitHub 연결 및 저장소 선택
4. 자동 배포

또는 드래그 앤 드롭:
1. Netlify 대시보드에서 "Add new site" → "Deploy manually"
2. 프로젝트 폴더를 드래그 앤 드롭

### Cloudflare Pages

**장점**: 전 세계 CDN, 빠른 속도, 무료 무제한 대역폭

1. [Cloudflare Pages](https://pages.cloudflare.com/)에 로그인
2. "Create a project" 클릭
3. GitHub 저장소 연결
4. 자동 배포

---

## 📝 배포 후 체크리스트

- [ ] 웹사이트가 정상적으로 로드되는지 확인
- [ ] 모든 이미지가 표시되는지 확인
- [ ] 네이버 지도가 작동하는지 확인
- [ ] 모바일에서 제대로 표시되는지 확인
- [ ] 전화/문자 링크가 작동하는지 확인
- [ ] 계좌번호 복사 기능이 작동하는지 확인
- [ ] 갤러리 모달이 정상 작동하는지 확인

---

## 🔧 문제 해결

### 지도가 표시되지 않는 경우

1. 네이버 클라우드 플랫폼에서 배포 도메인이 등록되었는지 확인
2. index.html의 Client ID가 올바른지 확인
3. 브라우저 개발자 도구(F12)의 Console 탭에서 에러 확인

### 이미지가 표시되지 않는 경우

- 이미지 경로가 상대 경로인지 확인 (`images/...`)
- GitHub에 이미지 파일이 제대로 푸시되었는지 확인

### 변경사항이 반영되지 않는 경우

- 브라우저 캐시 삭제 (Ctrl/Cmd + Shift + R)
- GitHub Actions 탭에서 배포 상태 확인
- 5-10분 정도 기다린 후 다시 확인

---

## 🎨 커스텀 도메인 연결 (선택사항)

### GitHub Pages에서 커스텀 도메인 사용

1. 도메인 구매 (가비아, 호스팅케이알 등)
2. DNS 설정에서 CNAME 레코드 추가:
   ```
   www -> YOUR_USERNAME.github.io
   ```
3. GitHub 저장소 Settings → Pages → Custom domain에 도메인 입력
4. "Enforce HTTPS" 체크

자세한 내용: https://docs.github.com/en/pages/configuring-a-custom-domain-for-your-github-pages-site

---

## 💡 팁

- **변경 후 즉시 배포**: Git push하면 1-2분 내에 자동 배포됩니다
- **무료 HTTPS**: 모든 옵션에서 자동으로 HTTPS가 제공됩니다
- **분석 추가**: Google Analytics를 추가하여 방문자 수를 확인할 수 있습니다
- **공유하기**: 짧은 URL이 필요하면 bit.ly나 tinyurl로 단축 URL 생성

---

## 📱 공유 방법

배포 후 다음 방법으로 공유하세요:

1. **카카오톡**: URL을 복사하여 전송
2. **문자**: 페이지의 "링크 복사" 버튼 사용
3. **QR 코드**: https://www.qr-code-generator.com/ 에서 생성
4. **SNS**: 페이지 링크 공유

---

## ⚡ 빠른 시작

가장 빠르게 배포하는 방법:

```bash
# 1. GitHub 저장소 생성 (웹에서)
# 2. 터미널에서 실행
git init
git add .
git commit -m "Wedding invitation website"
git remote add origin https://github.com/YOUR_USERNAME/wedding-invitation.git
git push -u origin main

# 3. GitHub 저장소 Settings → Pages에서 활성화
# 4. 완료! URL 확인
```

배포 완료 후 URL을 가족과 친구들에게 공유하세요! 🎉
